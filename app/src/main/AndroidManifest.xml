<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.vncoder.demo_layout">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".constraintlatout_4"></activity>
        <activity android:name=".MainItem" />
        <activity android:name=".ConstraintActivity" />
        <activity android:name=".MainView.editText" />
        <activity android:name=".MainView.CheckBoxx" />
        <activity android:name=".MainView.Chipp" />
        <activity android:name=".MainView.RadioGroup" />
        <activity android:name=".MainView.ImageVieww" />
        <activity android:name=".MainView.TextVieww" />
        <activity android:name=".MainView.Snackbarrr" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>